<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>FarmApp | index</title>
    </head>
    <body>
        <h1>商品一覧</h1>
        <% if (category !== 'All') { %>
        <h2>
            カテゴリー :
            <%= category %>
        </h2>
        <% } %>
        <ul>
            <% for (let product of products) { %>
            <li>
                <a href="/products/<%= product._id %>"><%= product.name %></a>
            </li>
            <% } %>
        </ul>
        <a href="/products/new">新規商品作成</a>
        <% if (category !== 'All') { %>
        <a href="/products/">商品一覧</a>
        <% } %>
    </body>
</html>
